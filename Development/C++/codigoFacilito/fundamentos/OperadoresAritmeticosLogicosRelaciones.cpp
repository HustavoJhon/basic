/*
 * Operaciones aritmeticas
 *   + suma
 *   - resta
 *   * multiplicacion
 *   / division
 *   % modulo
 *
 * Operaciones relacionales:
 *   == igual a
 *   != diferente de
 *   > mayor que
 *   < menor que
 *   >= mayor igual que
 *   <= menor igual que
 *
 * Operadores Logicos
 *   && and
 *   || or
 *   !  not
 */

#include <iostream>
using namespace std;

int main() { int a = 10, b = 5; }
